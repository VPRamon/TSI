services:
  app:
    image: tsi-devcontainer:dev
    build:
      context: ..
      dockerfile: docker/Dockerfile
      target: dev
    user: app
    working_dir: /workspace
    volumes:
      - ..:/workspace:cached
    command: sleep infinity
    environment:
      PYTHONPATH: /workspace/src
