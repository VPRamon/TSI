/**
 * Domain models - UI-specific view models and adapters.
 *
 * This layer separates API DTOs from UI concerns, providing:
 * - Computed properties (e.g., scheduling rate, formatted dates)
 * - UI-specific transformations
 * - Type-safe mapping from API responses
 */

export * from './schedule';
export * from './chart';
